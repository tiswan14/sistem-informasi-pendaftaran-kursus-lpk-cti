{
    "rewrites": [
        {
            "source": "/api/auth/(.*)",
            "destination": "/api/auth/[...nextauth]"
        }
    ],
    "build": {
        "env": {
            "NODE_ENV": "production"
        }
    }
}
